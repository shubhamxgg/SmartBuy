import CheckoutPage from "@/components/filtering/checkout-client";
export const metadata = {
  title: "Place Order",
  keywords: ["checkout", "order", "payment"],
  description: "Checkout page for your orders",
};

const Checkout = () => {
  return <CheckoutPage />;
};

export default Checkout;
