import WishlistPage from "@/components/wishlist/wishlist-client";

export const metadata = {
  title: "Your Wishlist",
  description: "Your wishlist items",
};

const Wishlist = () => {
  return <WishlistPage />;
};

export default Wishlist;
